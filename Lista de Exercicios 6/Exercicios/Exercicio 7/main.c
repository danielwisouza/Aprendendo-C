#include <stdio.h>
#include <stdlib.h>

int main()
{
    int *p, x=5;
    *p *= 2**p;
    //printf(" Tipo d em x %d\n", x);
    printf(" Tipo i em x %i\n", x);
    printf(" Tipo d em p %d\n", p);
    printf(" Tipo i em p %d\n", p);

    system('pause');
    return 0;
}
